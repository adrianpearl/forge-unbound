# Stripe API Keys
# Replace these with your actual Stripe keys from https://dashboard.stripe.com/apikeys

# Publishable key (used in frontend)
STRIPE_PUBLISHABLE_KEY=pk_test_dummy_key_replace_with_real_key

# Restricted key (used in backend - NEVER expose this to frontend)
STRIPE_RESTRICTED_KEY=sk_test_dummy_secret_key_replace_with_real_key

# Optional: Webhook endpoint secret for handling Stripe webhooks
STRIPE_WEBHOOK_SECRET=whsec_dummy_webhook_secret

# Server configuration
PORT=3000
NODE_ENV=development

# Database (if you want to store donation records)
# DATABASE_URL=postgresql://user:password@localhost:5432/donations

# Optional: Email service for receipts (using SendGrid as example)
# SENDGRID_API_KEY=your_sendgrid_api_key
# FROM_EMAIL=donations@yourdomain.com

# Optional: Analytics
# GOOGLE_ANALYTICS_ID=UA-XXXXXXXXX-X

# Campaign Configuration
# Basic Campaign Information
VITE_CAMPAIGN_NAME=Vega
VITE_CAMPAIGN_FULL_NAME=Vega for Congress
VITE_CAMPAIGN_TAGLINE=Support our campaign.
VITE_CAMPAIGN_WEBSITE=https://votevega.nyc/
VITE_CAMPAIGN_CONTACT_EMAIL=info@votevega.nyc

# Branding
VITE_CAMPAIGN_LOGO_URL=/assets/vega-red-black.png
VITE_CAMPAIGN_LOGO_ALT=Vega for Congress
# VITE_CAMPAIGN_PRIMARY_COLOR=#dc2626
# VITE_CAMPAIGN_SECONDARY_COLOR=#991b1b

# Legal Information
VITE_CAMPAIGN_LEGAL_NAME=Vega For Congress
# VITE_CAMPAIGN_FEC_ID=C00123456

# Donation Settings
VITE_CAMPAIGN_DEFAULT_AMOUNTS=[25, 50, 100, 250, 500, 1000, 3500]
VITE_CAMPAIGN_MAX_CONTRIBUTION=3500

# Custom Messaging (optional)
# VITE_CAMPAIGN_HEADER_TEXT=Help keep us independent. We take no corporate money. We're only backed by people like you.
# VITE_CAMPAIGN_SUCCESS_MESSAGE=Your contribution helps keep our campaign independent
